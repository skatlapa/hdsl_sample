---
infra:
  provision:
    cloud:
      openshift:
        instances:
        - name: testsecret
          namespace: myproject
          password: developer
          username: developer
          user: developer

  configure:
    playbooks:
      - location: playbooks/configuration/install_vim.yml

tests:
  playbooks:
    - location: playbooks/tests/verify_vim.yml
